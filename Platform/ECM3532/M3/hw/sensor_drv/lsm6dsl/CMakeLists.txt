GETCONFIG(lsmen "LSM6DSL_M3")
string(COMPARE EQUAL "${lsmen}" y _cmp)
if (_cmp)
file (GLOB lsm6dsl "${CMAKE_CURRENT_LIST_DIR}/*.c")
foreach (gsrc ${lsm6dsl})
    target_sources(${PROJECT_NAME}.elf PRIVATE ${gsrc})
endforeach()
target_include_directories(${PROJECT_NAME}.elf PUBLIC "${CMAKE_CURRENT_LIST_DIR}")
endif()

