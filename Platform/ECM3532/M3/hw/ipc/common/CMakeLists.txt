GETCONFIG(cfgvaren "RPC")
string(COMPARE EQUAL "${cfgvaren}" y _cmp)
if (_cmp)
    file (GLOB IPC "${CMAKE_CURRENT_LIST_DIR}/src/*.c")
    foreach (isrc ${IPC})
        target_sources(${PROJECT_NAME}.elf PRIVATE ${isrc})
    endforeach()
    add_subdirectory(include)
endif ()
