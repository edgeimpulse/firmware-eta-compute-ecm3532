GETCONFIG(cfgvaren "SERIAL_PROTOCOL")
string(COMPARE EQUAL "${cfgvaren}" y _cmp)
if (_cmp)
  if (EXISTS "${CMAKE_CURRENT_LIST_DIR}/src")
    add_subdirectory(src)
    add_subdirectory(include)
    add_subdirectory(xport)
  endif()
endif()
